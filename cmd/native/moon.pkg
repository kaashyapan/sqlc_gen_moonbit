import {
  "mizchi/sqlc_gen_moonbit/lib/codegen",
}

supported_targets = "native"

pkgtype(kind: "executable")

options(
  "native-stub": [ "stub.c" ],
  targets: {
    "ffi_native.mbt": [ "native" ],
    "ffi_stub.mbt": [ "wasm-gc", "wasm", "js" ],
  },
)
